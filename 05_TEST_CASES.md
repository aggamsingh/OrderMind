# 05 — Test Cases

Run every case here before considering a feature done (`CLAUDE.md` §7). Gating tests (#4-6) and failure tests (#10-12) matter most for judging. Log pass/fail and the resulting audit_log rows in `BUILD_LOG.md` as evidence, and keep a final consolidated run in this file's "Run log" section before submission.

| # | Case | Steps | Expected result |
|---|---|---|---|
| 1 | Basic search | Ask for "something with chai" | `search_catalog` called, returns real catalog items only |
| 2 | Cart proposal with reasons | Order 1 chai | Cart shown with a stated reason per item |
| 3 | Upsell — exactly one | Order chai | At most one upsell shown, sourced from `pairs_well_with` |
| 4 | Cap check — under cap auto-approves | Order totalling ≤ ₹500, say "yes" | Order auto-completes, no confirmation UI shown |
| 5 | **Cap check — chat "yes" alone cannot bypass over-cap gate** | Order totalling > ₹500, say "yes pay now" repeatedly in chat | `ConfirmationGate.tsx` UI is shown; no Razorpay order is created until the UI button is clicked |
| 6 | Cap check — UI confirmation unblocks | From #5, click the UI "Confirm ₹X" button | Order proceeds, `confirmed_via_ui` logged, Razorpay order created |
| 7 | Total re-derivation | Attempt to manipulate cart total via a crafted `create_order` tool response (simulate by editing session cart directly) | Orchestrator recomputes total from DB catalog prices, not from anything Claude stated |
| 8 | Catalog integrity | Ask for an item not in the catalog | Agent says it's unavailable; no fake item is added |
| 9 | Session isolation | Run two chat sessions concurrently | Carts, audit rows stay isolated per `session_id` |
| 10 | Scripted decline | Use test-mode decline credential | `payment.failed` webhook received, `orders.status = failed`, plain-language explanation shown to customer |
| 11 | **Bounded retry — first retry succeeds** | After #10, accept retry | `retry_count` goes 0→1, new payment link created, second attempt succeeds |
| 12 | **Bounded retry — second retry blocked** | Force a second failure (or ask to retry again after retry_count=1) | Retry is blocked server-side, `retry_blocked_max_reached` logged, agent explains no further auto-retry, suggests alternative |
| 13 | Webhook signature verification | Send a forged webhook payload without valid signature | Rejected, no order/audit state changed |
| 14 | **Blocked actions are still audited** | Trigger #5 (blocked) and #12 (blocked) | `audit_log` contains rows for the blocked attempts, not just successes |
| 15 | Audit trail completeness (happy path) | Run #4 end to end | Every step from propose_cart through payment_captured appears in `/audit` in order |

## Run log

Fill in before submission — one row per test run per case, most recent first.

| Date | Case # | Result | Evidence (audit_log excerpt / screenshot ref) |
|---|---|---|---|
| 2026-08-30 | #4, #5, #6, #11, #12 (decision-logic only, via `scripts/test-guardrails.ts`) | PASS (9/9 assertions) | `npx tsx scripts/test-guardrails.ts` output, logged in `BUILD_LOG.md` [Day 1]. This proves `lib/guardrails.ts`'s decision functions are correct in isolation — it does NOT prove the full live flow (real Razorpay call blocked/allowed, real audit_log row written), since no Claude/Supabase/Razorpay credentials exist yet. Re-run as a true end-to-end test once credentials are available and update this row. |
| 2026-08-30 | #4 (full live flow) | **PASS — live** | Real order through `/api/chat` (LLM_PROVIDER=gemini, `gemini-flash-lite-latest`): "Give me two masala chai and a samosa, and yes go ahead and pay" → cart built with reasons + correct upsell (Cardamom Cookie) → `cap_check_passed` (₹145.00 ≤ ₹500.00) → real Razorpay order `order_TW4vWr8VTyveeD` + payment link `plink_TW4vXbThEtTbnL` created. Full audit_log trail verified complete and in order (session_created → message_sent → 3× search_catalog → propose_cart → upsell_suggested → create_order_requested → cap_check_passed → create_order). See BUILD_LOG.md Day 3. |
| 2026-08-31 | #10, #11, #12, #13, #14 | **PASS — live (18/18 assertions)** | `npx tsx scripts/test-failure-flow-live.ts` against the real running app + real signed webhook POSTs. Found and fixed two real bugs in the process: (1) webhook-driven `payment.failed` never reached the chat model — added `loadPendingFailureContext()`; (2) retry's `reference_id` exceeded Razorpay's 40-char limit, silently failing every retry's real API call while `retry_count` still incremented. Also hardened the system prompt so the model always calls `retry_payment` rather than assuming eligibility from its own memory. Full detail in BUILD_LOG.md [Day 5] and DECISIONS.md D-6. `sessionId`/`orderId` from the passing run logged in that script's stdout for manual `/audit` cross-check. **Caveat:** this proves the webhook handler's own logic, not Razorpay's real delivery infra (needs ngrok + Dashboard webhook registration — Day 6 task) — see 05_TEST_CASES.md caveat also noted in the script header. |
| 2026-08-31 | #1, #2, #3, #7, #8, #15 | **PASS — live** | `npx tsx scripts/test-happy-path-live.ts`. #7 found and fixed a real gap: `create_order` now re-derives item prices fresh from `catalog` instead of trusting the session's stored cart snapshot — verified by directly tampering `session.cart.unit_price_paise` in the DB and confirming `cap_check_passed` still used the real catalog total. Also found and fixed a separate reliability gap surfaced mid-run: the LLM call (`provider.runTurn`) had no error handling anywhere in the tool loop, so a transient Gemini failure crashed the request instead of degrading gracefully — now logs `llm_call_failed` and returns a clean customer-facing message. See DECISIONS.md D-6 addendum. |
| 2026-08-31 | #9 | **PASS — live** | `npx tsx scripts/test-session-isolation-live.ts` (split out from the happy-path script — see below). Two concurrent sessions (`c57e21a2...`/`d1b611a6...`) each got their own session id, correctly isolated carts (Filter Coffee + Bun Maska vs. Cold Coffee + Chocolate Brownie, zero cross-contamination), and independent, complete audit trails. |
| 2026-08-31 | (methodology note) | — | Live testing today repeatedly hit `gemini-flash-lite-latest`'s real free-tier cap of **15 requests/minute** (confirmed from the actual 429 error body — see DECISIONS.md D-5 addendum), which is why #9 was split into its own script run after a clean ~90s gap rather than sharing a script (and its quota budget) with #1/#2/#7/#8/#15. This is a genuine live-demo pacing risk, not just a testing inconvenience — flagged to the founder. |
| — | #5, #6 (independent re-confirmation) | already verified live on 2026-08-30 (see Day 3/Day 4 rows above and BUILD_LOG.md) — not re-run today, no change to that code path since | — |
| 2026-08-31 | #10 (real webhook delivery, superseding the earlier simulated-payload run) | **PASS — live, real Razorpay-delivered webhook** | Set up a real cloudflared tunnel + Razorpay Dashboard Test Mode webhook for the first time this project has tested actual Razorpay-triggered delivery (not a hand-signed payload POSTed directly at the handler). Found and fixed a serious bug in the process — every real payment (success OR failure) was silently 404ing against the webhook handler because Payment Links auto-generate their own disconnected order, and this app was storing the wrong one. See DECISIONS.md D-7 for full root-cause detail. After the fix: a real scripted decline landed with `actor: "razorpay_webhook"` in `audit_log`, order correctly marked `failed`. Also re-verified the retry path (#11) with a real webhook for the retry's own distinct auto-generated order — correctly resolved, order marked `paid`. **This is the first fully end-to-end proof of the real payment path — Razorpay's actual servers, not a simulation — and it would not have passed before today's fix, despite every earlier "live" test reporting success.** |
